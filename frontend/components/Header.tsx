'use client';

// This file is superseded by Navbar.tsx which provides full mega-menu
// and mobile drawer support. Keeping this as a thin re-export so any
// lingering imports don't break the build.
export { default } from '@/components/Navbar';
