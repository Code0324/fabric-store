'use client';

// This is an alias kept for backward compatibility.
// All new code should import from @/components/ProductCard instead.
export { default as ProductCard } from '@/components/ProductCard';
export { default } from '@/components/ProductCard';
